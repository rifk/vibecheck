# iOS App Launcher

The shared Kotlin UI entrypoint is `com.vibecheck.MainViewController` from `composeApp`.

You can create an Xcode project that embeds the generated framework from `composeApp` and hosts this view controller.
